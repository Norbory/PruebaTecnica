import { TelefonoPipePipe } from './telefono-pipe.pipe';

describe('TelefonoPipePipe', () => {
  it('create an instance', () => {
    const pipe = new TelefonoPipePipe();
    expect(pipe).toBeTruthy();
  });
});
